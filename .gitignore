node_modules/

# Build output
dist/
.next/
out/
build/

# Env files (each workspace's own .env is gitignored individually too)
.env
.env.local
.env.*.local

# Logs
npm-debug.log*
*.log

# OS / editor
.DS_Store
.vscode/
.idea/

# Prisma generated client
src/generated/prisma/

# Locally installed community agent skills (npx skills add), not committed.
# The .agents/skills content is covered generally; each symlink under
# .claude/skills still needs its own line (gitignore can't match "symlinks only").
.agents/skills/
.claude/skills/better-auth-best-practices
.claude/skills/vercel-react-best-practices
